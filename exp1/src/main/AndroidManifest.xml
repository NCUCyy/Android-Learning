<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Exp">
        <activity
                android:name=".diceGame.DiceHistoryActivity"
                android:exported="false" />
        <activity
                android:name=".diceGame.Test"
                android:exported="false" />
        <activity
                android:name=".diceGame.GameLoseActivity"
                android:exported="false" />
        <activity
                android:name=".diceGame.GameWinActivity"
                android:exported="false" />
        <activity
                android:name=".diceGame.GameActivity"
                android:exported="false" />
        <activity
                android:name=".diceGame.MainActivity"
                android:exported="true" />
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:label="@string/app_name"
                android:theme="@style/Theme.Exp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>